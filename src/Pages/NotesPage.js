import React from 'react';
import NoteList from '../Components/Notes/NoteList';

const NotesPage = () => {
    return (
        <div>
            <NoteList/>
        </div>
    );
}

export default NotesPage;
